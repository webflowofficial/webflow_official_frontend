import React from "react";

function SectionHeading({ children }) {
    return <p className="text-3xl font-bold leading-none text-custom-primary-font md:text-4xl">{children}</p>;
}

export default SectionHeading;
//  lg:text-5xl
